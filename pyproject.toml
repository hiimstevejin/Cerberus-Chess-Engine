[project]
name = "chess"
version = "0.1.0"
description = "Light weight chess engine made with python-chess"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-chess>=1.2.0",
]
